<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.appiaries.demo"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="8" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" android:debuggable="true">
        <activity
            android:name=".appiariesChat.activities.GlobalTimelineActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".appiariesChat.activities.UserTimelineActivity" android:label="@string/menu_user_timeline"></activity><activity android:name=".appiariesChat.activities.AllUsersActivity" android:label="@string/menu_all_users"></activity>
        <activity android:label="@string/menu_my_favorites" android:name=".appiariesChat.activities.FavoriteUsersActivity"></activity>
        <activity android:name=".appiariesChat.activities.PreferenceActivity" android:label="@string/menu_preference"></activity>
        
    </application>

</manifest>